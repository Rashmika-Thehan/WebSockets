# To test the WebSocket server, you need to install wscat using following command,
    npm install -g wscat

# you can use the following command in your terminal to connect to the WebSocket server:
    ws -c ws://localhost:8080